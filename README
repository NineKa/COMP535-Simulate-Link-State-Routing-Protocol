Configure Entry added to configure file:
socs.network.router.processIP   : represent the simulated process IP of router
socs.network.router.processPort : represent the simulated port number of router

How to build:
mvn compile assembly:single

How to run:
java -cp [generated jar file] socs.network.Main [configure file]
This will start a simuation based on the configure file, for example

java -cp COMP535.jar socs.network.Main router2.conf

Implemented Command:
-   attach [processIP] [processPort] [Situated IP] [weight]
        e.g. attach 127.0.0.1 9002 192.168.2.1 1
-   start
-   neighbors
